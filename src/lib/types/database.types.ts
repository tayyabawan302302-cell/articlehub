// This is a placeholder. After running the migration against your real
// Supabase project, regenerate real types with:
//
//   npm run gen:types
//
// (requires the Supabase CLI and SUPABASE_PROJECT_ID set in your shell)
//
// Until then this loose type keeps the app compiling; swap it out before
// shipping so you get full column-level type safety and autocomplete.
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
